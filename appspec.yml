version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACE_ME_TASK_DEF"    # GitHub Actions에서 대체됨
        LoadBalancerInfo:
          ContainerName: "moyeorak-container"
          ContainerPort: 8080
